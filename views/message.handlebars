<div class="card" hx-target="this" hx-swap="outerHTML">
  <div class="card-content">{{message.author}}:
    <p>{{message.content}}</p>
    <button hx-get="/message/{{message.id}}/edit">edit</button>
  </div>
</div>